<install>
  <copy dir="${package.root}/install/bundles" todir="${env.bundles}" overwriteIfNewerVersion="true" />
  <copy dir="${package.root}/install/templates" todir="${env.templates}" overwriteIfNewerVersion="true" />
</install>
