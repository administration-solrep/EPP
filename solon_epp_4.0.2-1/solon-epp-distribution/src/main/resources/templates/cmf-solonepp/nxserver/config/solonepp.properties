# Configuration du bulk mode
nuxeo.bulk.mode=${nuxeo.bulk.mode}

# Configuration du serveur LDAP
nuxeo.ldap.url=${nuxeo.ldap.url}
nuxeo.ldap.bindDn=${nuxeo.ldap.bindDn}
nuxeo.ldap.password=${nuxeo.ldap.password}
nuxeo.ldap.cacheTimeout=${nuxeo.ldap.cacheTimeout}

# Configuration du serveur de mail (envoi)
mail.transport.protocol=${mail.transport.protocol}
mail.transport.host=${mail.transport.host}
mail.transport.port=${mail.transport.port}
mail.smtp.auth=${mail.smtp.auth}
mail.transport.user=${mail.transport.user}
mail.transport.password=${mail.transport.password}
mail.from=${mail.from}

# Configuration du serveur de mail (réception)
mail.store.protocol=${mail.store.protocol}
mail.user=${mail.user}
mail.host=${mail.host}
mail.store.port=${mail.store.port}
mailservice.user=${mailservice.user}
mailservice.password=${mailservice.password}
mail.debug=${mail.debug}

# Paramétrage des traitements batch
nuxeo.batch.user=${nuxeo.batch.user}
nuxeo.batch.password=${nuxeo.batch.password}

# Parametres de l'application EPP
syndication.list.size=${syndication.list.size}

# Parametres de l'application numero de version (voir nuxeo.conf)
reponses.product.tag=%%SNAPSHOT%%